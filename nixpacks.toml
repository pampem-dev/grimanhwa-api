[phases.setup]
nixPkgs = ["python311", "google-chrome", "chromedriver"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python manage.py collectstatic --noinput && gunicorn backend.wsgi"